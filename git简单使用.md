```shell
初始化
git init
git remote add origin ${github仓库地址}

克隆 Github 仓库到本地:
git clone ${github仓库地址}

拉取远程仓库：
git pull <remote> <branch>

将变更同步到 Github
git add .
git commit -m "版本信息"
git push --set-upstream rigin master
```

